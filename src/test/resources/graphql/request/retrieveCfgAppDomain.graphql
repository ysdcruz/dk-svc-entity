query app {
    app(domain: "staging.tickets.splashisland.ph") {
        name
        description
        email
        facebookLink
        twitterLink
        appUrl
        paypalWebProfile
        isActive
        domains
        logoUrl
        iconUrl
        contactUsUrl
        smtpServer
        smtpPort
        smtpUsername
        smtpPassword
    }
}