query app {
    app(domain: "non-existing-domain") {
        name
        description
        email
        facebookLink
        twitterLink
        appUrl
        paypalWebProfile
        isActive
        domains
        logoUrl
        iconUrl
        contactUsUrl
        smtpServer
        smtpPort
        smtpUsername
        smtpPassword
    }
}