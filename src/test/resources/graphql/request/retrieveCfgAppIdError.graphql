query app {
    app(pkAppId: 99999) {
        name
        description
        email
        facebookLink
        twitterLink
        appUrl
        paypalWebProfile
        isActive
        domains
        logoUrl
        iconUrl
        contactUsUrl
        smtpServer
        smtpPort
        smtpUsername
        smtpPassword
    }
}