query app {
    app(merchantId: "sportskunekk") {
        name
        description
        email
        facebookLink
        twitterLink
        appUrl
        paypalWebProfile
        isActive
        domains
        logoUrl
        iconUrl
        contactUsUrl
        smtpServer
        smtpPort
        smtpUsername
        smtpPassword
    }
}