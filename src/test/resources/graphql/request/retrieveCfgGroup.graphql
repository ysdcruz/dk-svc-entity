query group {
    group(pkGroupId: 1) {
        name
        description
        image
        coverPhoto
        dialingCode
        contactDetailsForm
        latitude
        longitude
        currency
        isCommunity
        isActive
    }
}